@model E_Exam.Models.StudentAnswer

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>StudentAnswer</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="ExamQuestionId" class="control-label"></label>
                <select asp-for="ExamQuestionId" class="form-control" asp-items="ViewBag.ExamQuestionId"></select>
                <span asp-validation-for="ExamQuestionId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AnswerId" class="control-label"></label>
                <select asp-for="AnswerId" class="form-control" asp-items="ViewBag.AnswerId"></select>
                <span asp-validation-for="AnswerId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

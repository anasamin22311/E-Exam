@model E_Exam.Models.QuestionStatistics

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>QuestionStatistics</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="CorrectAnswers" class="control-label"></label>
                <input asp-for="CorrectAnswers" class="form-control" />
                <span asp-validation-for="CorrectAnswers" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="WrongAnswers" class="control-label"></label>
                <input asp-for="WrongAnswers" class="form-control" />
                <span asp-validation-for="WrongAnswers" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
